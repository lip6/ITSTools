install: true

language: java

script : cd fr.lip6.move.gal.parent && mvn generate-sources install && cd - 

jdk: oraclejdk8

env:
  global:
    - MAVEN_OPTS="-XX:MaxPermSize=512m -Xmx4g"

cache:
  directories:
    - '$HOME/.m2/repository'

  
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  local_dir: fr.lip6.move.gal.updatesite/target/repository
  on:
    branch: master
